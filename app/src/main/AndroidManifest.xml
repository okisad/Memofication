<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.oktaysadoglu.memofication"
          xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".Memofication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity android:name=".activities.mainActivity.MainActivity">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER"/>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.gameActivity.GameActivityReverse"
            android:label="@string/title_activity_placement_test"
            android:parentActivityName=".activities.mainActivity.MainActivity"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name=".activities.gameActivity.GameActivityStraight"
            android:label="@string/title_activity_placement_test"
            android:parentActivityName=".activities.mainActivity.MainActivity"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".activities.TestActivity">
        </activity>
    </application>

</manifest>